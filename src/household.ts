import Pool from '../config/dbConnect';

interface Household {
    
};

export const typeDefs = `#graphql
    type Mutation {
    }
    type Query {
    }
`;

export const resolvers = {
    Query: {
    },
    Mutation: {
    }
};